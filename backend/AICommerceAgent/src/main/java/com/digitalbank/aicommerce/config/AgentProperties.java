package com.digitalbank.aicommerce.config;

/**
 * Tunables for the agent: model id, effort, conversation limits, the largest
 * amount that may be proposed, and how long a proposal stays confirmable.
 */
public class AgentProperties {

    // TODO: bind from configuration under an "agent." prefix.
}
