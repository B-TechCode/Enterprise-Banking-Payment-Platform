package com.digitalbank.aicommerce.domain;

/**
 * One row per tool invocation, forming the AI audit trail.
 */
public class AgentActionLog {

    // TODO: id, conversationId, customerId, subject, toolName, inputJson,
    // TODO: outcome, resultSummary, downstreamStatus, correlationId, occurredAt.
    // TODO: JPA mapping arrives with the schema.
}
