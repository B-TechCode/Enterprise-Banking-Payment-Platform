# Tests excluded from CI, each for a stated reason. Remove a line once the test
# can pass on a bare runner: this is a to-do list, not a place to hide tests.
# test-summary.py prints this list on every run so it stays visible.

# Need Config Server + Postgres: with neither available there is no datasource.
# Pass against the docker compose stack; fail on a runner with no infrastructure.
**/AccountServiceApplicationTests.java
**/BillPayWorkerServiceApplicationTests.java

# Needs Config Server: auth.service.url is defined only in the config repo, so
# without it the Feign client is built with an unresolved ${...} URL.
**/CusomerServiceApplicationTests.java

# BROKEN regardless of environment: Spring Initializr leftovers in package
# com.example.demo that cannot find the application class. These need fixing,
# not infrastructure.
**/BillPaymentServiceApplicationTests.java
**/PaymentOrchestratorApplicationTests.java
