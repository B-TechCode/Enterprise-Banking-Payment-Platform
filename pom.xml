<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.digitalbank</groupId>
    <artifactId>digital-banking-platform</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <packaging>pom</packaging>

    <name>Digital Banking Platform</name>

    <description>
        Enterprise Digital Banking Platform based on Spring Boot Microservices
    </description>

    <!-- ===================================================== -->
    <!-- Global Properties -->
    <!-- ===================================================== -->

    <properties>

        <java.version>21</java.version>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <spring.boot.version>3.3.1</spring.boot.version>

        <spring.cloud.version>2023.0.1</spring.cloud.version>

        <mapstruct.version>1.5.5.Final</mapstruct.version>

        <lombok.version>1.18.32</lombok.version>

        <postgresql.version>42.7.2</postgresql.version>

        <auth0.jwt.version>4.4.0</auth0.jwt.version>

        <springdoc.version>2.6.0</springdoc.version>

        <!-- Shared Libraries -->

        <commons.dto.version>1.0.0</commons.dto.version>

        <commons.security.version>1.0.0</commons.security.version>

        <commons.observability.version>1.0.0</commons.observability.version>

    </properties>

    <!-- ===================================================== -->
    <!-- Modules -->
    <!-- ===================================================== -->

    <modules>

        <module>backend/Config-Server</module>

        <module>backend/API-Gateway</module>

        <module>backend/AuthUser-develop</module>

        <module>backend/CustomerService-develop</module>

        <module>backend/AccountService-develop</module>

        <module>backend/BillerService-develop</module>

        <module>backend/PaymentOrchestrator-develop</module>

        <module>backend/BillPayWorkerService-develop</module>

        <module>backend/SettlementService-develop</module>

        <module>backend/commons-dto-develop</module>

        <module>backend/commons-security-develop</module>

        <module>backend/commons-observability-develop</module>

    </modules>

    <!-- ===================================================== -->
    <!-- Dependency Management -->
    <!-- ===================================================== -->

    <dependencyManagement>

        <dependencies>

            <!-- Spring Boot -->

            <dependency>

                <groupId>org.springframework.boot</groupId>

                <artifactId>spring-boot-dependencies</artifactId>

                <version>${spring.boot.version}</version>

                <type>pom</type>

                <scope>import</scope>

            </dependency>

            <!-- Spring Cloud -->

            <dependency>

                <groupId>org.springframework.cloud</groupId>

                <artifactId>spring-cloud-dependencies</artifactId>

                <version>${spring.cloud.version}</version>

                <type>pom</type>

                <scope>import</scope>

            </dependency>

            <!-- Shared Libraries -->

            <dependency>

                <groupId>com.commons</groupId>

                <artifactId>commons-dto</artifactId>

                <version>${commons.dto.version}</version>

            </dependency>

            <dependency>

                <groupId>com.commons</groupId>

                <artifactId>commons-security</artifactId>

                <version>${commons.security.version}</version>

            </dependency>

            <dependency>

                <groupId>com.commons</groupId>

                <artifactId>commons-observability</artifactId>

                <version>${commons.observability.version}</version>

            </dependency>

            <!-- MapStruct -->

            <dependency>

                <groupId>org.mapstruct</groupId>

                <artifactId>mapstruct</artifactId>

                <version>${mapstruct.version}</version>

            </dependency>

            <dependency>

                <groupId>org.mapstruct</groupId>

                <artifactId>mapstruct-processor</artifactId>

                <version>${mapstruct.version}</version>

            </dependency>

            <!-- PostgreSQL -->

            <dependency>

                <groupId>org.postgresql</groupId>

                <artifactId>postgresql</artifactId>

                <version>${postgresql.version}</version>

            </dependency>

            <!-- JWT -->

            <dependency>

                <groupId>com.auth0</groupId>

                <artifactId>java-jwt</artifactId>

                <version>${auth0.jwt.version}</version>

            </dependency>

            <!-- Swagger -->

            <dependency>

                <groupId>org.springdoc</groupId>

                <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>

                <version>${springdoc.version}</version>

            </dependency>

        </dependencies>

    </dependencyManagement>

    <!-- ===================================================== -->
    <!-- Plugin Management -->
    <!-- ===================================================== -->

    <build>

        <pluginManagement>

            <plugins>

                <!-- Java Compiler -->

                <plugin>

                    <groupId>org.apache.maven.plugins</groupId>

                    <artifactId>maven-compiler-plugin</artifactId>

                    <version>3.13.0</version>

                    <configuration>

                        <release>${java.version}</release>

                    </configuration>

                </plugin>

                <!-- Spring Boot -->

                <plugin>

                    <groupId>org.springframework.boot</groupId>

                    <artifactId>spring-boot-maven-plugin</artifactId>

                    <version>${spring.boot.version}</version>

                </plugin>

            </plugins>

        </pluginManagement>

    </build>

</project>